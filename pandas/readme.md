# 🐼 Pandas

This folder contains my hands-on practice, interview preparation, and notes for the **Pandas** library in Python.

## 📖 About

Pandas is a powerful Python library used for data manipulation, cleaning, analysis, and visualization preparation. This notebook covers beginner to intermediate concepts with practical examples and interview-focused explanations.

---

## 📂 Files

- 📓 **Pandas.ipynb** - Complete Pandas practice notebook

---

## 🚀 Topics Covered

### 1. Series
- Creating Series
- Custom Index
- Accessing Elements

### 2. DataFrame
- Creating DataFrame
- Selecting Rows & Columns
- Indexing

### 3. Reading & Writing Files
- `read_csv()`
- `read_excel()`
- `to_csv()`
- `to_excel()`

### 4. Data Inspection
- `head()`
- `tail()`
- `sample()`
- `shape`
- `columns`
- `index`
- `dtypes`
- `info()`
- `describe()`

### 5. Data Selection
- Single Column
- Multiple Columns
- `loc[]`
- `iloc[]`

### 6. Filtering
- Boolean Indexing
- Multiple Conditions
- `isin()`
- `between()`

### 7. Sorting
- `sort_values()`
- `sort_index()`

### 8. Missing Values
- `isnull()`
- `notnull()`
- `isna()`
- `fillna()`
- `dropna()`

### 9. Duplicate Handling
- `duplicated()`
- `drop_duplicates()`

### 10. Statistics
- `mean()`
- `median()`
- `mode()`
- `sum()`
- `count()`
- `min()`
- `max()`
- `std()`

### 11. Grouping & Aggregation
- `groupby()`
- `agg()`

### 12. Data Analysis Functions
- `value_counts()`
- `unique()`
- `nunique()`

### 13. Data Transformation
- `apply()`
- `map()`
- `applymap()`
- `replace()`
- `astype()`

### 14. Combining Data
- `merge()`
- `concat()`
- `join()`

### 15. Reshaping Data
- `pivot_table()`
- `crosstab()`

### 16. Querying Data
- `query()`

### 17. Sampling
- `sample()`

---

## 💻 Technologies Used

- Python 3
- Pandas
- NumPy
- Jupyter Notebook

---

## 🎯 Purpose

This notebook was created to:

- Learn Pandas from basics to intermediate level.
- Practice real-world data manipulation.
- Prepare for Python and Pandas interview questions.
- Build a Data Analytics portfolio.

---

## 📌 Future Improvements

- Real-world datasets
- Data Cleaning Projects
- Exploratory Data Analysis (EDA)
- Time Series Analysis
- Advanced Pandas Techniques

---

## ⭐ If you find this repository helpful, feel free to star it!
